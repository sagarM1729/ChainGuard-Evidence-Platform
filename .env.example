# =============================================================================
# ChainGuard Evidence Platform - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# TIER 1: PostgreSQL Database (Neon Cloud)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# TIER 2: Merkle Evidence Ledger (no environment variables required)

# TIER 3: IPFS Storage (Pinata)
# Provide either a JWT or an API key/secret pair. JWT is recommended for simplicity.
PINATA_JWT=your_pinata_jwt_here
# PINATA_API_KEY=your_pinata_api_key_here
# PINATA_API_SECRET=your_pinata_api_secret_here
# PINATA_GATEWAY_URL=https://gateway.pinata.cloud/ipfs

# Email Service
RESEND_API_KEY=your_resend_api_key_here

# Development
NODE_ENV=development